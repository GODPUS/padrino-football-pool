<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:picks, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:picks, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :pick, url(:picks, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'picks/form', :locals => { :f => f } %>
 <% end %>
</div>
